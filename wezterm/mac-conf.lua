function setup(config)
    config.font_size = 13
    config.send_composed_key_when_left_alt_is_pressed = false -- Left Opt as Meta
    config.send_composed_key_when_right_alt_is_pressed = true -- Right Opt like AltGr
end

return setup
